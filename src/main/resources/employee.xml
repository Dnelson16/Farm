
<mapper namespace="org.farm.dao.IEmployeeDao">
    <resultMap id="employeeResultMap" type="org.farm.dao.Models.Employee">
        <id property="employeeId" column="employee_id" />
        <result property="farmId" column="farm_id" />
        <result property="name" column="name" />
        <result property="position" column="position" />
        <result property="salary" column="salary" />
        <result property="farmerId" column="farmer_id" />
    </resultMap>

    <select id="getAllEmployees" resultMap="employeeResultMap">
        SELECT * FROM employees
    </select>

    <select id="getEmployeeById" resultMap="employeeResultMap" parameterType="int">
        SELECT * FROM employees WHERE employee_id = #{employeeId}
    </select>

    <insert id="insertEmployee" parameterType="org.farm.dao.Models.Employee">
        INSERT INTO employees (farm_id, name, position, salary, farmer_id)
        VALUES (#{farmId}, #{name}, #{position}, #{salary}, #{farmerId})
    </insert>

    <update id="updateEmployee" parameterType="org.farm.dao.Models.Employee">
        UPDATE employees SET farm_id = #{farmId}, name = #{name}, position = #{position},
        salary = #{salary}, farmer_id = #{farmerId} WHERE employee_id = #{employeeId}
    </update>

    <delete id="deleteEmployee" parameterType="int">
        DELETE FROM employees WHERE employee_id = #{employeeId}
    </delete>
</mapper>
